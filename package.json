{
  "name": "monotest",
  "private": true,
  "packageManager": "yarn@3.0.0",
  "workspaces": [
    "apps/*",
    "libs/*"
  ],
  "scripts": {
    "build": "yarn workspace monotest run build",
    "start": "yarn workspace @monotest/main start"
  }
}
